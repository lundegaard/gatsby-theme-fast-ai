import React from 'react';
import { Box } from '@fast-ai/ui-components';

const ContentContainer = props => (
	<Box data-testid="content-container" variant="content" {...props} />
);

export default ContentContainer;
