## Code

Inline `code`

Indented code

    // Some comments
    line 1 of code
    line 2 of code
    line 3 of code

Block code "fences"

```
Sample text here...
```

Javascript

```js
var foo = function(bar) {
	return bar++;
};

console.log(foo(5));
```

Java

```java
Jaxenter out = null;
try {
    out = new Jaxenter (new FileWriter(”filename”, true));
    out.write(”aString”);
} catch (IOException e) {
    // error processing code
} finally {
    if (out != null) {
        out.close();
    }
}
```

JSON

```json
{
	"maxWidth": "100%",
	"overflowX": "auto",
	"overflowY": "hidden"
}
```
