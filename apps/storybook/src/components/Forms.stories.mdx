import {
	Text,
	TextField,
	SelectField,
	SliderField,
	CheckboxField,
	SuperField,
	Col,
	Container,
	Radio,
	RadioGroupField,
	RadioGroup,
	Row,
} from '@fast-ai/ui-components';
import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { Field } from 'react-powerplug';

<Meta title="Examples/Forms" component={SuperField} />

## Sample form

<Preview>
	<Story name="Form">
		<Container>
			<Row py={2}>
				<Col span={6}>
					<Field>
						{({ set, value }) => (
							<TextField
								label="Name"
								name="name"
								onChange={(event) => set(event.target.value)}
								value={value}
							/>
						)}
					</Field>
				</Col>
				<Col span={6}>
					<Field>
						{({ set, value }) => (
							<TextField
								label="Surname"
								name="surname"
								onChange={(event) => {
									set(event.target.value);
								}}
								value={value}
								hint="Hint how to fill your surname"
								placeholder="Fill in a surname"
							/>
						)}
					</Field>
				</Col>
			</Row>
			<Row py={2}>
				<Col span={12}>
					<Field>
						{({ set, value }) => (
							<TextField
								label="Personal ID"
								name="id"
								onChange={(event) => set(event.target.value)}
								value={value}
								hint="with hint"
							/>
						)}
					</Field>
				</Col>
			</Row>
			<Row py={2}>
				<Col span={12}>
					<Field initial={[10, 90]}>
						{({ set, value }) => (
							<SliderField
								label="Amount"
								name="amount"
								min={0}
								max={100}
								step={10}
								onChange={(newValue) => {
									set(newValue);
								}}
								value={value}
							/>
						)}
					</Field>
				</Col>
			</Row>
			<Row py={2}>
				<Col span={6}>
					<Field>
						{({ set, value }) => (
							<SelectField
								label="Education"
								name="education"
								onChange={(event) => set(event.target.value)}
								value={value}
								placeholder="None"
								items={[
									{ value: '1', label: 'Elementary' },
									{ value: '2', label: 'High-school' },
								]}
								hint="Your highest education"
							/>
						)}
					</Field>
				</Col>
				<Col span={6}>
					<Field>
						{({ set, value }) => (
							<SelectField
								label="Education"
								name="education"
								onChange={(event) => set(event.target.value)}
								value={value}
								items={[
									{ value: '', label: '' },
									{ value: '1', label: 'Elementary' },
									{ value: '2', label: 'High-school' },
								]}
								hint="Your highest education"
							/>
						)}
					</Field>
				</Col>
			</Row>
			<Row py={2}>
				<Col span={12}>
					<Field>
						{({ set, value }) => (
							<CheckboxField
								label={
									<Text fontSize={[1, 1, 1, 2]} p={0} m={0}>
										{' '}
										I agreee with terms and conditions
									</Text>
								}
								name="terms"
								checked={value}
								onChange={(event) => set(event.target.checked)}
								value
							/>
						)}
					</Field>
				</Col>
			</Row>
			<Row>
				<Col span={12}>
					<Field>
						{({ set, value }) => (
							<RadioGroupField
								legend="Who is your partner?"
								onChange={(event) => set(event.target.value)}
								name="partner"
								value={value}
							>
								{[
									{ value: 'individual', label: 'Individual' },
									{ value: 'with-parner', label: 'With partner' },
								].map((itemProps) => (
									<Radio key={itemProps.value} {...itemProps} />
								))}
							</RadioGroupField>
						)}
					</Field>
				</Col>
			</Row>
		</Container>
	</Story>
</Preview>

## Radio

<Props of={Radio} />

## RadioGroup

<Props of={RadioGroup} />

## RadioGroupField

<Props of={RadioGroupField} />
