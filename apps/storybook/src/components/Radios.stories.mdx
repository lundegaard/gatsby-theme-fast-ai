import { Col, Container, Radio, RadioGroupField, RadioGroup, Row } from '@fast-ai/ui-components';
import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { Field } from 'react-powerplug';

<Meta title="Radios" component={RadioGroupField} subcomponents={[Radio, RadioGroup]} />

## Groups

<Preview>
	<Story name="Groups">
		<Container>
			<Row p={2}>
				<Col span={6}>
					<Field>
						{({ value, set }) => (
							<RadioGroupField
								legend="Who is your partner?"
								onChange={event => set(event.target.value)}
								name="partner"
								value={value}
							>
								{[
									{ value: 'individual', label: 'Individual' },
									{ value: 'with-parner', label: 'With partner' },
								].map(itemProps => (
									<Radio key={itemProps.value} {...itemProps} />
								))}
							</RadioGroupField>
						)}
					</Field>
				</Col>
			</Row>
		</Container>
	</Story>
</Preview>

<Preview>
	<Story name="Groups - disabled">
		<Container>
			<Row p={2}>
				<Col span={6}>
					<Field>
						{({ value, set }) => (
							<RadioGroupField
								legend="Who is your partner?"
								onChange={event => set(event.target.value)}
								name="partner"
								value={value}
								disabled
							>
								{[
									{ value: 'individual', label: 'Individual' },
									{ value: 'with-parner', label: 'With partner' },
								].map(itemProps => (
									<Radio key={itemProps.value} {...itemProps} />
								))}
							</RadioGroupField>
						)}
					</Field>
				</Col>
			</Row>
		</Container>
	</Story>
</Preview>

<Preview>
	<Story name="Groups - errors">
		<Container>
			<Row p={2}>
				<Col span={6}>
					<Field>
						{({ value, set }) => (
							<RadioGroupField
								legend="Who is your partner?"
								onChange={event => set(event.target.value)}
								name="partner"
								value={value}
								hasError
								hint="error"
							>
								{[
									{ value: 'individual', label: 'Individual' },
									{ value: 'with-parner', label: 'With partner' },
								].map(itemProps => (
									<Radio key={itemProps.value} {...itemProps} />
								))}
							</RadioGroupField>
						)}
					</Field>
				</Col>
			</Row>
		</Container>
	</Story>
</Preview>

## Radio

<Props of={Radio} />

## RadioGroup

<Props of={RadioGroup} />

## RadioGroupField

<Props of={RadioGroupField} />

